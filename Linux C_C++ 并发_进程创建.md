在 Linux C/C++ 编程中，进程创建是并发与系统编程的基石。理解进程的创建、内存布局、文件描述符继承以及生命周期管理，是编写健壮后台服务的前提。

下面我将从核心的 `fork` 函数切入，详细剖析进程创建的各个环节及底层原理。

---

### 一、 核心函数：`fork`

`fork` 是 Linux 中创建新进程的唯一传统方式（除极早期的 `vfork` 外）。它的特点是 **“调用一次，返回两次”**。

```c
#include <unistd.h>
pid_t fork(void);
```

#### 1. 返回值机制
*   **返回 `-1`**：失败。常见原因包括：系统进程数达到上限（`EAGAIN`）、内存不足（`ENOMEM`）。
*   **返回 `0`**：当前处于**子进程**中。子进程可以通过 `getpid()` 获取自己的 PID，通过 `getppid()` 获取父进程 PID。
*   **返回 `> 0`**：当前处于**父进程**中。返回值即为新创建的子进程的 PID。

#### 2. 底层机制：写时复制
在早期的 Unix 中，`fork` 会完全复制父进程的整个物理内存空间，这对于大内存程序极其低效。现代 Linux 采用 **写时复制** 技术：
*   `fork` 时，内核只复制父进程的页表，并将父子进程的页表项都标记为**只读**。
*   父子进程共享同一份物理内存，直到其中一方尝试修改某块内存（触发缺页异常 Page Fault）。
*   此时，内核才会为修改内存的那一方分配新的物理页，并复制数据过去，将页表项重新标记为可读写。
*   **意义**：极大地减少了内存分配和复制的开销。即使父进程占用数十 GB 内存，`fork` 也能在毫秒级完成。

---

### 二、 父子进程的细节与继承关系

`fork` 之后，子进程是父进程的几乎完全相同的副本。理解“哪些被继承了，哪些没被继承”至关重要。

#### 1. 被继承的属性
*   **内存空间**：数据段、BSS段、堆、栈、代码段均通过 COW 机制共享副本。
*   **文件描述符表**：父进程打开的所有文件描述符（FD）都会被复制到子进程中。**注意：是共享文件表项**。这意味着父子进程共享同一个文件偏移量。如果子进程 `lseek` 了文件，父进程的读写位置也会变。
*   **环境变量**。
*   **工作目录与根目录**。
*   **信号处理函数的设置**（信号屏蔽字也会继承）。
*   **控制终端**。
*   **资源限制**（如最大文件大小、CPU 时间限制等）。

#### 2. 未被继承的属性
*   **PID 和 PPID**：子进程有自己唯一的 PID，其 PPID 为父进程的 PID。
*   **文件锁**：父进程持有的记录锁（`fcntl`），子进程不会继承。
*   **未决信号**：子进程的未决信号集会被清空。
*   **闹钟**：`alarm` 设置的定时器会被清零。
*   **CPU 时间统计**：子进程的 CPU 时间（用户态/内核态）归零。

---

### 三、 结束进程

进程的终止分为正常退出和异常终止。

#### 1. 退出函数
```c
#include <stdlib.h>
void exit(int status);   // 标准库退出
```
*   `exit` 会执行由 `atexit()` 和 `on_exit()` 注册的清理函数。
*   刷新并关闭所有标准 I/O 流（如 `stdio.h` 中的 `FILE*`，会调用 `fflush`）。
*   删除由 `tmpfile()` 创建的临时文件。
*   最后调用系统调用 `_exit()`。

```c
#include <unistd.h>
void _exit(int status);  // 系统调用退出
```
*   直接陷入内核，**不执行**上述用户态的清理工作。
*   **最佳实践**：在 `fork` 后的子进程中，如果不明确接管父进程的 I/O 和清理逻辑，建议使用 `_exit()` 而不是 `exit()`。否则可能会触发父进程尚未刷新的标准 I/O 缓冲区，导致输出重复或混乱。

#### 2. 进程状态
*   **僵尸进程**：子进程先于父进程结束，但父进程没有调用 `wait/waitpid` 回收。此时子进程的 PID、退出状态等仍保留在内核进程表中，占用系统资源。大量僵尸进程会耗尽系统 PID 资源。
*   **孤儿进程**：父进程先于子进程结束。子进程被 `init` 进程（PID 为 1，现代 Linux 中可能是 `systemd` 或特定 subreaper）收养。孤儿进程不会造成危害，因为 `init` 会在它们结束时自动调用 `wait` 回收。

#### 3. 回收子进程
父进程有责任回收子进程的退出状态：
```c
#include <sys/wait.h>
pid_t wait(int *status);          // 阻塞等待任意一个子进程
pid_t waitpid(pid_t pid, int *status, int options); // 可以指定 PID，支持非阻塞 (WNOHANG)
```
通过 `WIFEXITED(status)`、`WEXITSTATUS(status)` 等宏可以解析子进程的退出码或终止信号。

---

### 四、 拓展与补充

#### 1. `exec` 家族：变身
`fork` 本身只是复制，通常在 `fork` 之后，子进程会立刻调用 `exec` 函数加载新的程序代码替换当前进程映像。
```c
#include <unistd.h>
extern char **environ;

int execl(const char *path, const char *arg, ... /* (char  *) NULL */);
int execv(const char *path, char *const argv[]);
int execvp(const char *file, char *const argv[]); // 查找 PATH 环境变量
// ... 还有 execle, execvpe 等
```
*   **exec 的特性**：如果成功，当前进程的代码段、数据段、堆栈全部被新程序替换。但 **PID 不变**，且 **打开的文件描述符默认保留**（除非设置了 `FD_CLOEXEC` 标志）。
*   **Linux 特有 API `memfd_create` + `exec`**：可以在内存中创建匿名文件，写入可执行代码后直接 `exec`，无需落盘，常用于无文件攻击或沙箱执行。

#### 2. `vfork` 与 `clone`（进阶底层）
*   **`vfork`**：早期的优化手段。`vfork` 创建子进程时**不复制页表**，子进程共享父进程的内存空间。子进程被严格限制：必须立刻调用 `exec` 或 `_exit`，不能修改任何变量，否则行为未定义（会破坏父进程内存）。现代 Linux 由于 COW 的存在，`vfork` 已极少使用，除非在极端嵌入式环境或特殊的协程实现中。
*   **`clone`**：Linux 真正的底层进程/线程创建系统调用。
    ```c
    long clone(unsigned long flags, void *child_stack, int *ptid, int *ctid, unsigned long newtls);
    ```
    `fork`、`vfork`、以及 C++ 的 `std::thread`（基于 `pthread_create`）在底层都是调用 `clone`。
    通过控制 `flags`（如 `CLONE_VM` 共享内存空间，`CLONE_FILES` 共享文件描述符表，`CLONE_SIGHAND` 共享信号处理），`clone` 可以精确控制父子进程之间共享什么资源。共享内存和 FD 表的进程，其实就是我们常说的**线程**。

#### 3. 实战避坑指南：`fork` 与多线程
这是一个 C/C++ 高频面试题与实战大坑：**在多线程程序中调用 `fork`**。
*   **规则**：`fork` 只复制调用 `fork` 的那一个线程，其他线程在子进程中凭空消失。
*   **致命问题**：如果其他线程在 `fork` 时正持有锁（如 `malloc` 内部的全局锁、`printf` 的锁），`fork` 后子进程中该锁仍然处于锁定状态，但持有锁的线程不见了（死锁）。如果子进程随后调用 `printf` 或 `malloc`，会立即死锁。
*   **解决方案**：多线程程序 `fork` 后，子进程唯一安全的操作是调用 `_exit()` 或立刻调用 `exec` 执行新程序。绝对不要调用非异步信号安全的函数。
*   如果必须在不 `exec` 的情况下在子进程做点事，可以使用 `pthread_atfork()` 机制在 `fork` 的各个阶段加解锁，但这非常复杂且容易出错。

#### 4. C++ 视角的补充
在 C++ 中使用 `fork` 需要特别小心对象生命周期：
1.  **析构函数不会跨进程调用**：父进程中的栈对象在 `fork` 后，子进程也会有一份副本。子进程退出时，会执行这些副本的析构函数。这可能导致重复释放资源（如析构函数中 `close(fd)` 关闭了父进程还在用的 FD）。
2.  解决思路：在子进程中尽量只用原生系统调用，或者设计专门的子进程执行类，在 `fork` 后立即脱离父进程的对象管理体系。

#### 5. 现代 C++ 进程管理库
由于原生系统调用过于底层且易错，现代 C++ 项目通常使用第三方库来管理子进程，例如：
*   **Boost.Process**：提供跨平台的、面向对象的进程管理，支持同步/异步执行、管道重定向。
*   **Google RE2/subprocess**：轻量级的子进程封装。

### 总结

进程创建的核心在于 `fork` 的 COW 机制和返回值逻辑。父子进程在文件描述符和内存上的共享关系是系统编程的基础。而在实际高并发网络编程中，我们往往采用 `fork` + `exec` 的模式运行外部程序，或者在多进程网络模型（如 Nginx 的 Pre-fork 模型）中利用父子进程共享监听 Socket (`SO_REUSEPORT`) 来实现并发。
