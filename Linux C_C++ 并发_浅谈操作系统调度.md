### Q: 每个程序执行变成进程，每个进程分配<=thread::hardware_concurrency() 数目的线程保持并行运行; 但这么多程序，操作系统是如何保证并行的？是否因为时间片轮转紧缺变成并发？
你的直觉非常敏锐，直接触及了操作系统调度的本质！

直接回答你的问题：**是的！当系统中所有程序开启的线程总数远大于 CPU 物理核心数时，严格的“并行”就会因为物理资源的限制，退化成宏观上并行、微观上交替执行的“并发”。**

为了彻底厘清这个问题，我们需要把视角从“单个程序的自我优化”切换到“操作系统的全局统筹”。

---

### 一、 概念纠偏：并行 vs 并发

*   **并行**：物理上的同时执行。只有当 CPU 核心数 $\ge$ 运行线程数时才可能发生。比如 4 核 CPU 上跑 4 个死循环线程，它们是真正在同一个物理时刻滴答运行的。
*   **并发**：逻辑上的同时处理。当线程数 > CPU 核心数时，操作系统通过快速切换时间片，让你“感觉”它们在同时运行。

### 二、 操作系统的全局视角：它才不管你分配了几个线程

你提到“每个进程分配 $\le$ `thread::hardware_concurrency()` 数目的线程”。这是一个非常好的**应用级最佳实践**，目的是防止单个应用把系统资源榨干。

但是，对操作系统来说，**它只看全局**：
假设你的服务器有 8 个核心。系统里运行着 10 个程序，每个程序都很乖，只创建了 8 个线程。
那么全局来看，系统中就有 $10 \times 8 = 80$ 个活跃线程。

80 个线程抢 8 个核心，物理上不可能同时运行。此时操作系统的调度器就开始展现它的魔力了。

### 三、 操作系统是如何保证“并发”且不卡顿的？

Linux 内核的完全公平调度器（CFS）通过以下机制完美处理了这个矛盾：

#### 1. 时间片轮转
内核将一秒钟切分成极小的时间片（例如 1ms 到 10ms）。
8 个核心就像 8 个窗口，80 个线程排着队。核心运行一个线程几毫秒后，强行打断它，保存它的上下文（寄存器状态、栈指针），然后切换到下一个线程。
因为切换速度极快（微秒级），人类和上层应用程序根本感觉不到中断，产生了一种“所有 80 个线程都在同时运行”的幻觉。这就是你说的“时间片轮转紧缺变成并发”。

#### 2. 动态优先级与公平性
操作系统不会让某个线程一直霸占 CPU。CFS 调度器会跟踪每个线程的“虚拟运行时间”。运行时间越长的线程，优先级越会被调低，从而让那些很少运行的线程（如等待 I/O 的线程）能迅速拿到时间片。

#### 3. 区分 CPU 密集型与 I/O 密集型
这也是操作系统能保持流畅的核心秘密。
*   **I/O 密集型线程**：比如网络服务等待读数据、读磁盘。这种线程在等待时是**阻塞睡眠**的，根本不占 CPU 时间片。当数据到达时，内核通过中断唤醒它，它就能立刻拿到时间片干活。网络服务器中 99% 的线程都在睡眠，这就是为什么 80 个线程在 8 核上跑完全不卡的原因。
*   **CPU 密集型线程**：比如死循环计算圆周率。如果 80 个线程全都是这种，那 8 核 CPU 会被 100% 打满，系统会感到明显卡顿，因为大量时间被浪费在无意义的“上下文切换”上了。

### 四、 为什么要限制 `hardware_concurrency()`？

既然操作系统有时间片轮转，为什么我们写程序时还要限制每个程序创建的线程数不超过 CPU 核心数呢？让操作系统去切不行吗？

**不行，因为上下文切换是有代价的！**

1.  **切换开销**：每次切换线程，CPU 需要保存当前线程状态、加载新线程状态。这会导致 CPU 缓存失效、TLB 刷新。如果线程过多，系统把时间全浪费在切换上，实际干活的净时间反而变少。
2.  **内存开销**：每个线程默认需要分配几 MB 的栈空间。80 个线程还好，如果是 10000 个线程，光栈空间就要吃掉几十 GB 内存。
3.  **锁竞争加剧**：线程越多，它们争抢同一个锁的概率越大，导致大量线程在锁的等待队列里睡眠，白白消耗内存资源。

### 五、 进阶补充：现代高并发服务器的终极解法

在真实的 C++ 高性能后台开发中（如 Nginx, Netty, muduo 网络库），面对海量并发连接，如果用“每个连接分配一个线程”的模型，哪怕限制在 `hardware_concurrency()`，也处理不了几万个并发连接（因为线程阻塞在慢速 I/O 上，利用率不高）。

现代操作系统的解法是 **Reactor 模式 + 异步 I/O (epoll)**：
1.  **只创建 `hardware_concurrency()` 个线程**。
2.  **没有任何线程阻塞等待 I/O**。一个线程通过 `epoll` 机制同时监控成千上万个网络 Socket。
3.  **事件驱动**：哪个 Socket 有数据来了，操作系统通知这个线程去处理。处理完继续监控。
4.  这样一来，8 核 CPU 跑 8 个线程，这 8 个线程几乎永远不会进入睡眠阻塞状态，它们一直在满负荷干活。既实现了真正的物理并行，又处理了海量并发。

**总结：**
单个程序自我限制线程数是为了不拖垮系统；但在系统全局视角下，线程总数必定大于核心数，依靠时间片轮转将“并行”转化为高效的“并发”，而 I/O 多路复用技术则是让 CPU 尽量少做无谓的线程切换、榨干性能的终极武器。
