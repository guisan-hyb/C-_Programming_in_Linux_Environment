在 Linux C/C++ 网络编程中，UDP（用户数据报协议）与 TCP 有着截然不同的设计哲学。TCP 是面向连接的、可靠的字节流；而 UDP 是**无连接的、不可靠的、面向数据报**的协议。

正因为没有建立连接（无三次握手）和维护连接状态的开销，UDP 的 API 极其简洁，且在某些实时性要求高（如视频流、DNS、游戏）或多播/广播场景中不可替代。

下面详细解析 UDP 网络编程的核心 API 及其底层语义。

---

### 一、 核心通信流程图

UDP 通信非常直接，没有明确的“服务端”和“客户端”的严格界限，通常我们称双方为“发送端”和“接收端”。

```text
   接收端 (通常绑定固定端口)             发送端
   -----------------------            --------------
 1. socket(AF_INET, SOCK_DGRAM, 0)  1. socket(AF_INET, SOCK_DGRAM, 0)
 2. bind()                           (可选: bind())
 3. recvfrom() <--- 数据报文 --- 2. sendto()
 4. sendto()   ---> 数据报文 ---> 3. recvfrom()
 5. close()                          4. close()
```

---

### 二、 核心 API 详解

#### 1. `socket()`：创建 UDP 套接字
```c
int socket(int domain, int type, int protocol);
```
*   **用法**：与 TCP 唯一的区别在于 `type` 参数。
*   **参数**：`type` 必须设为 `SOCK_DGRAM`（数据报套接字）。`protocol` 通常传 `0`，内核会自动选择 UDP 协议。
    ```c
    int sockfd = socket(AF_INET, SOCK_DGRAM, 0);
    ```

#### 2. `bind()`：绑定地址（接收端必做）
```c
int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
```
*   **用法**：接收端必须 `bind` 到一个固定的 IP 和端口，否则发送端不知道往哪里发。
*   **发送端**：通常不需要显式 `bind`。当发送端首次调用 `sendto` 时，内核会自动为其分配一个临时端口并完成隐式绑定。

#### 3. `sendto()`：发送数据报
```c
ssize_t sendto(int sockfd, const void *buf, size_t len, int flags,
               const struct sockaddr *dest_addr, socklen_t addrlen);
```
*   **作用**：向指定的目的地址发送数据。
*   **核心参数 `dest_addr`**：因为是“无连接”的，所以每次发送都必须明确告诉内核数据要发给谁（IP + Port）。
*   **返回值**：返回实际发送的字节数。**注意：UDP 的写操作没有“写半包”的概念。** 如果成功返回，意味着整个数据报已经完整交给了内核缓冲区（准备发往网络）。如果数据报大小超过内核发送缓冲区限制，会直接返回错误 `EMSGSIZE`。

#### 4. `recvfrom()`：接收数据报
```c
ssize_t recvfrom(int sockfd, void *buf, size_t len, int flags,
                 struct sockaddr *src_addr, socklen_t *addrlen);
```
*   **作用**：在指定的套接字上等待数据到达，并获取发送方的地址。
*   **核心参数 `src_addr`**：这是一个出参。当数据到达时，内核会把**发送方的 IP 和端口**填入这个结构体。这使得接收端知道是谁发来的数据，从而可以用 `sendto` 进行回复。
*   **返回值**：返回实际接收的字节数。
*   **避坑（截断问题）**：如果到达的数据报大小为 2000 字节，而你提供的 `len` 只有 1024 字节，在 Linux 默认情况下，内核只会拷贝前 1024 字节，**剩下的 976 字节会被直接丢弃！** （可以通过设置 `MSG_TRUNC` 标志来改变此行为，获取真实报文长度，但数据依然被截断）。

---

### 三、 进阶补充：UDP 的“连接”状态 (`connect`)

在 UDP 编程中，有一个看似矛盾的操作：对 UDP 套接字调用 `connect()`。

```c
int connect(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
```

**对 UDP 调用 `connect` 不会发送任何网络包（无三次握手）！** 它只是在内核中为这个套接字绑定了一个“默认的目的地址”。

**为什么要对 UDP 调用 `connect`？**

1.  **简化代码（用 `send`/`write` 代替 `sendto`）**：
    调用 `connect` 后，可以直接使用 `send(sockfd, buf, len, flags)` 或 `write()` 发送数据，不再需要每次都传入冗长的目的地址结构体。
2.  **提升性能（减少内核态开销）**：
    未 `connect` 的 UDP 套接字，每次调用 `sendto`，内核都要做以下动作：连接状态检查 -> 路由查询 -> 绑定源端口 -> 发送。如果高频发送，开销很大。`connect` 后，内核只做一次这些动作，后续的 `send` 性能大幅提升。
3.  **感知错误（异步错误）**：
    如果 UDP 发送的数据报到达了目的主机，但目的端口没有进程在监听，目的主机会回发一个 ICMP "Port Unreachable" 报文。
    *   **未 `connect`**：内核收到 ICMP 错误后，由于无法知道是哪个 `sendto` 发出的，通常会将错误丢弃。应用层根本不知道发送失败了。
    *   **已 `connect`**：内核明确知道这个套接字绑定了哪个目的地址，当 ICMP 错误到达时，内核会将其保存在套接字的错误队列中。应用层下一次调用 `send` 或 `recv` 时，会收到 `ECONNREFUSED` 错误。

---

### 四、 UDP 网络编程实战陷阱与对比

#### 1. 绝无“粘包”问题
TCP 是字节流，有粘包；**UDP 是数据报，有明确边界。**
发送端调用一次 `sendto` 发送 100 字节，接收端调用一次 `recvfrom` 就会精确收到这 100 字节。绝不会发生两次 `sendto` 的数据合并在一次 `recvfrom` 中返回的情况。

#### 2. 可靠性需应用层保证
UDP 不保证送达，不保证顺序。如果网络拥塞，路由器直接丢弃 UDP 包。
在工程实践中，如果业务需要可靠性（如文件传输），开发者必须在应用层自行实现：**超时重传、ACK 确认、序列号去重、流量控制**。这也是 QUIC 协议（HTTP/3 底层）的思路：在 UDP 之上在用户态重新实现类似 TCP 的可靠机制。

#### 3. MTU 与分片问题
以太网的 MTU（最大传输单元）通常是 1500 字节。去掉 IP 头（20 字节）和 UDP 头（8 字节），UDP 的**最佳载荷大小应控制在 1472 字节以内**。
如果一次 `sendto` 发送 8000 字节，IP 层会将其拆分为多个分片发送。如果其中任何一个分片在网络中丢失，整个 8000 字节的数据报都会被接收端丢弃。**在 UDP 应用中，强烈建议将单包大小限制在 1472 以内，降低整包丢失率。**

#### 4. 广播与多播
UDP 天生支持一对多通信，这是 TCP 无法做到的。
*   **广播**：设置 `setsockopt(sockfd, SOL_SOCKET, SO_BROADCAST, &on, sizeof(on));`，目的地址设为 `255.255.255.255`，同一局域网内的所有主机都能收到。
*   **多播（组播）**：更高效的一对多通信，只有加入了特定多播组（如 `224.0.0.1`）的主机才会接收数据，常用于视频会议流媒体。

### 总结
UDP API 表面上只有 `sendto` 和 `recvfrom`，极其简单。但它的难点不在 API 调用本身，而在于**如何应对不可靠的网络环境**，以及如何处理**数据报边界、分片限制**和**流控问题**。在 C++ 高并发框架中，UDP 常与 `epoll` 结合，实现高性能的实时数据分发服务。

