当你编写一个程序，并在其中通过系统调用（如 `fork()`）创建了一个新进程时，你的原程序进程与新进程之间就建立了一种**父子关系**。

你的原程序进程被称为**父进程**，新创建的进程被称为**子进程**。它们之间的关系不仅限于“谁创建了谁”，而是体现在资源继承、执行顺序、生命周期和通信等多个维度。

以下是父子进程之间核心关系的详细拆解：

---

### 1. 身份与层级关系

*   **PID（进程ID）与 PPID（父进程ID）**：
    子进程拥有由内核分配的唯一 PID。同时，子进程的 PPID 就是父进程的 PID。
*   **进程组与会话**：
    默认情况下，子进程继承父进程的**进程组 ID (PGID)** 和 **会话 ID (SID)**。这意味着父子进程属于同一个进程组，会接收到相同的终端信号（如 `Ctrl+C` 产生的 `SIGINT`），除非子进程通过 `setsid()` 脱离出去自立门户。

### 2. 资源继承关系（共享与独立）

这是最容易被初学者误解的地方：子进程并不是完全的“空壳”，也不是和父进程完全共享一切。

*   **内存空间（独立副本，写时复制）**：
    子进程获得父进程内存空间的**完全副本**（代码段、数据段、堆、栈）。在现代 Linux 中，这通过 **写时复制** 实现：初始时共享物理内存，谁修改谁才真正复制。**结论：父子进程的内存是相互独立的，修改自己的全局变量或堆内存不会影响对方。**
*   **文件描述符（共享文件表项）**：
    这是极其重要的一点。如果父进程在 `fork` 之前打开了文件（或网络 Socket），子进程会继承这些文件描述符。
    *   **关系**：父子进程的 FD 指向内核中的**同一个打开文件表项**。这意味着它们**共享同一个文件偏移量**。如果子进程读了 10 个字节，父进程接着读会从第 11 个字节开始。如果不需要共享，子进程通常会在 `exec` 前设置 `FD_CLOEXEC` 关闭它们。
*   **环境变量与工作目录（继承）**：
    子进程继承父进程的环境变量表和工作目录。但子进程修改自己的环境变量或调用 `chdir` 不会影响父进程。

### 3. 执行流与并发关系

*   **并发执行**：`fork()` 调用成功后，父子进程都在运行，它们共享 CPU 时间片，是并发执行的。
*   **执行顺序不确定**：`fork` 返回后，是父进程先运行，还是子进程先运行，取决于操作系统的调度器，是不确定的。如果需要控制顺序，必须使用**进程间通信（IPC）**（如管道、信号量）或 `wait` 机制进行同步。
*   **代码分支**：它们从 `fork` 调用点之后开始分道扬镳，通常通过判断 `fork` 的返回值（父进程返回子进程 PID，子进程返回 0）来执行不同的代码块。

### 4. 生命周期与生死相依

父子进程在生命周期上存在强绑定关系，主要体现在“回收”机制上：

*   **僵尸进程**：如果**子进程先结束**，内核不会立刻彻底销毁它，而是保留它的 PID、退出状态和资源使用统计，直到父进程调用 `wait()` 或 `waitpid()` 来读取这些信息。在父进程读取之前，子进程被称为**僵尸进程**。如果父进程一直不回收，大量僵尸进程会耗尽系统 PID 资源。
*   **孤儿进程**：如果**父进程先结束**，子进程还在运行，子进程就成了**孤儿进程**。Linux 的机制是：孤儿进程会被 `init` 进程（PID 为 1，或现代的 `systemd`）收养。`init` 会在孤儿进程结束时自动调用 `wait` 回收它，因此孤儿进程是无害的。

### 5. 权限关系

*   子进程继承父进程的**实际用户 ID (UID)**、**实际组 ID (GID)**、**有效用户 ID (EUID)** 和 **有效组 ID (EGID)**。
*   通常情况下，父子进程权限相同。除非父进程以 root 身份运行，并且设置了 SUID 权限位等特殊情况，子进程的权限可能会有所变化。

### 6. 通信关系

因为父子进程的内存是隔离的，它们不能直接通过变量互相通信。如果它们需要协作，必须使用内核提供的**进程间通信** 机制：
*   **管道**：最常用，通常在 `fork` 之前创建，子进程继承。
*   **共享内存**：让父子进程映射同一块物理内存。
*   **消息队列、信号量**。
*   **信号**：可以互相发送异步信号（如父进程用 `kill(pid, SIGTERM)` 杀死子进程）。

### 总结比喻

你可以把父子进程的关系类比为：
**父亲（父进程）克隆了一个自己（子进程）。**
1. 克隆体拥有本体当时的记忆（内存副本）和财产钥匙（文件描述符）。
2. 但从分裂那一刻起，克隆体和本体各自独立生活，各自赚钱各自花，互不干涉内存（独立变量修改）。
3. 但如果他们共用同一个银行账户（共享文件表项），一个人取了钱，账户余额对两人都变了。
4. 本体（父）有责任在克隆体（子）死掉时去处理其后事（回收僵尸进程）；如果本体先死了，克隆体就成了孤儿，由国家来收养。

