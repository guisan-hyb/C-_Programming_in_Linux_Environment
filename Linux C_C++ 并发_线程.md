按照你提供的大纲，我将为你详细梳理 Linux C/C++ 中关于**线程**的核心知识体系。线程是 Linux 并发编程的绝对主力，理解它的底层机制对于编写高性能、无 Bug 的后端服务至关重要。

以下是详细的讲解与拓展：

---

### 一、 线程概念

**什么是线程？**
*   **传统视角**：进程是程序执行的实例，是资源分配的基本单位。而线程是进程内的一个执行流，是**CPU调度的基本单位**。
*   **Linux 底层视角（独特且重要）**：在 Linux 内核中，其实并没有严格的“进程”和“线程”之分。Linux 内核只认识“任务”。线程在底层是通过 `clone()` 系统调用创建的，它本质上是一个**与父进程共享内存空间和文件描述符表的轻量级进程**。
*   **通俗比喻**：如果把进程比作一个工厂（拥有厂房、机器等资源），那么线程就是工厂里的工人。多个工人共享工厂的资源，协同工作。

### 二、 线程特点

1.  **轻量级**：创建和销毁的开销远小于进程。因为不需要复制内存空间（页表）和文件描述符表。
2.  **并发性**：在多核 CPU 上，同一进程内的多个线程可以真正并行执行；在单核上通过时间片轮转并发执行。
3.  **共享性**：同一进程内的线程间通信极其高效，因为它们共享同一块虚拟地址空间。可以直接读写全局变量。
4.  **数据一致性风险**：共享带来便利的同时，也引入了竞态条件，必须使用锁机制来保护共享数据。

### 三、 线程共享资源

同一个进程内的多个线程，以下资源是**共享**的：
*   **内存空间**：
    *   **代码段**：所有线程执行的是同一份代码。
    *   **数据段 / BSS段**：全局变量和静态变量是共享的。
    *   **堆区**：`malloc` / `new` 分配的内存对所有线程可见。这也是内存分配器（如 `glibc` 的 `ptmalloc`）必须加锁或使用线程局部缓存的原因。
*   **文件描述符表**：一个线程 `open` 了一个文件，其他线程也能通过同一个 `fd` 进行读写。
*   **信号处理函数**：信号处理函数是进程级别的，对所有线程生效。
*   **当前工作目录**和**用户 ID / 组 ID**。

### 四、 线程私有资源

虽然共享了大部分资源，但为了保证各自独立执行，线程也有自己的**私有**资源：
*   **线程 ID (TID)**：在进程内唯一标识一个线程。
*   **寄存器集合**：包括程序计数器（PC，记录下一条要执行的指令）和栈指针（SP）。这保证了每个线程的执行流是独立的。
*   **栈空间**：每个线程都有自己独立的栈（默认通常是 8MB），用于存储局部变量、函数调用链等。
*   **错误码**：C 语言中的 `errno` 是每个线程独有的，防止一个线程的系统调用出错覆盖了另一个线程的 `errno`。
*   **信号掩码**：每个线程可以独立屏蔽某些信号。
*   **拓展：线程局部存储**：一种特殊的机制，允许全局变量在每个线程中都有一个独立的副本。

### 五、 Linux 线程库

*   **历史演进**：早期的 Linux 2.4 内核使用 `LinuxThreads` 库，它实现得很粗糙（甚至每个线程有不同的 PID）。现代 Linux（2.6 内核以后）统一使用 **NPTL (Native POSIX Thread Library)**。在 NPTL 下，同一进程内的所有线程拥有相同的 PID（即 TGID，线程组ID），内核内部通过 `task_struct` 中的 `pid` 字段区分各个线程。
*   **编程接口**：POSIX 标准定义了 `pthread` 库。在 Linux C/C++ 编程中，我们通常链接 `-lpthread`。在现代 glibc (2.34+) 中， pthread 已被整合进 libc，不再需要显式链接。
*   **C++ 视角**：C++11 引入了 `std::thread`，它是跨平台的线程封装。在底层，Linux 上的 `std::thread` 依然是调用 `pthread_create`。

### 六、 线程创建

使用 `pthread_create` 创建线程：

```c
#include <pthread.h>
int pthread_create(pthread_t *thread, const pthread_attr_t *attr,
                   void *(*start_routine) (void *), void *arg);
```
*   `thread`：返回线程 ID。
*   `attr`：线程属性（如设置分离状态、栈大小），传 `NULL` 使用默认属性。
*   `start_routine`：线程要执行的函数指针。
*   `arg`：传给线程函数的参数。

**注意点**：传递参数 `arg` 时，如果是传地址，必须确保该变量的生命周期在子线程使用它时依然有效。如果传局部变量的地址，极易引发竞态条件。

### 七、 线程回收

与进程的僵尸进程类似，线程如果不被回收，其退出状态和少量栈信息会残留在系统中，成为**未分离线程**，导致资源泄漏。

*   **`pthread_join` (阻塞回收)**：
    ```c
    int pthread_join(pthread_t thread, void **retval);
    ```
    *   阻塞调用线程，直到目标线程结束。
    *   可以获取目标线程的退出返回值。
    *   调用后内核自动清理被回收线程的残留资源。
*   **`pthread_detach` (分离态，自动回收)**：
    ```c
    int pthread_detach(pthread_t thread);
    ```
    *   将线程设置为分离状态。分离状态的线程结束时，系统会**自动回收**其资源。
    *   一旦分离，就不能再对该线程调用 `pthread_join`。
    *   **最佳实践**：在网络服务器中，如果主线程不需要关心子线程的返回值，通常在创建子线程后立即将其 `detach`，或者在子线程首行调用 `pthread_detach(pthread_self())`。

### 八、 线程结束

线程结束有以下几种方式：
1.  **自然死亡**：线程函数执行完毕，或者执行了 `return`。
2.  **主动退出**：在线程内部调用 `pthread_exit(void *retval)`。`retval` 可被 `pthread_join` 获取。
3.  **被动杀死**：被其他线程调用 `pthread_cancel(pthread_t thread)` 取消。被取消的线程可以设置取消状态（异步取消或延迟取消）。
4.  **进程级别结束**：任意一个线程调用了 `exit()`，或者主线程从 `main` 函数 `return`，会导致**整个进程**的所有线程瞬间死亡。

### 九、 拓展与补充（进阶必知）

#### 1. C++ 中的线程生命周期与 RAII
在 C++ 中使用 `std::thread` 时，如果 `std::thread` 对象析构时线程仍然在运行（既没有 `join` 也没有 `detach`），程序会调用 `std::terminate()` 崩溃。现代 C++ 推荐使用 RAII 包装器（如 C++20 的 `std::jthread`，或自己实现），在析构时自动 `join`。

#### 2. 线程安全与可重入
*   **线程安全**：函数可以被多个线程同时调用，不会产生数据竞争或错误结果。通常靠加锁实现。
*   **可重入**：函数在执行过程中被中断，再次被调用（如信号处理中调用），依然能正确运行。可重入函数必定是线程安全的，但线程安全函数不一定是可重入的（比如内部用了锁，如果被信号中断再次获取同一个锁会死锁）。

#### 3. 多线程与 `fork` 的致命毒药（重点避坑）
前面提到过，`fork` 只复制调用它的那一个线程。如果父进程是一个多线程程序，且其他线程在 `fork` 时正持有锁（比如 `malloc` 的内存分配锁），`fork` 后子进程中该锁依然处于锁定状态，但持有锁的线程不见了（死锁）。
**铁律**：多线程程序中调用 `fork` 后，子进程**唯一安全**的操作是立刻调用 `exec` 执行新程序。绝对不要在子进程中调用任何非异步信号安全的函数（如 `printf`, `malloc`, `syslog`）。

#### 4. 线程崩溃对进程的影响
如果某个线程发生了段错误（如访问空指针导致 `SIGSEGV`），内核会向**整个进程**发送致命信号，导致进程及其下的所有线程一起崩溃。线程之间没有内存隔离，这也是线程模型相比多进程模型不够健壮的地方。

#### 5. 线程的调度与亲和性
在高性能网络编程中，为了减少 CPU 缓存失效，可以通过 `pthread_setaffinity_np` 将某个线程绑定到特定的 CPU 核心上运行（CPU 亲和性），避免线程在多核间频繁迁移。

### 总结

线程是 Linux 并发编程的利器，以极低的上下文切换开销和高效的共享内存通信成为主流。掌握**共享与私有资源的边界**是写出正确并发程序的前提；而熟练运用**同步机制（互斥锁、条件变量等）**来保护共享资源，则是进阶高级 C/C++ 开发者的必经之路。

