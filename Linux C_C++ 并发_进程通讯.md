由于各个进程拥有独立的虚拟内存空间，进程间无法像线程那样直接通过全局变量通信，必须通过内核提供的机制来实现。以下是详细的讲解与拓展：

---

### 一、 进程间通信（IPC）方式概述

Linux 提供了多种 IPC 机制，每种都有其特定的应用场景和历史背景：
1.  **管道**：包括无名管道和有名管道，本质是内核内存中的一段缓冲区。
2.  **信号**：一种异步通信机制，用于通知进程发生了某种事件。
3.  **消息队列**：消息的链表，存放在内核中，克服了管道的无格式字节流和缓冲区大小限制。
4.  **共享内存**：允许多个进程映射同一块物理内存，是**最快的 IPC 方式**。
5.  **信号量**：主要用于进程间同步，常与共享内存配合使用。
6.  **网络 Socket**：可用于跨主机通信，也可通过 Unix Domain Socket 实现本机高效通信。

---

### 二、 管道

管道是最古老的 IPC 方式，其本质是内核维护的一块内存缓冲区。管道的一个核心特性是**半双工**（数据只能单向流动）。

#### 1. 无名管道
*   **特点**：
    *   只能用于具有**亲缘关系**的进程间通信（通常是父子进程或兄弟进程）。
    *   没有实体文件，只存在于内核内存中。
    *   本质是字节流，无边界。
*   **API**：`int pipe(int fd[2]);`
    *   `fd[0]` 固定用于读端。
    *   `fd[1]` 固定用于写端。
*   **经典使用模型**：`fork` 之后，父子进程各自关闭不需要的一端（父关读，子关写），形成单向通道。
*   **行为特性**（极其重要）：
    *   如果读端全部关闭，写端继续写，内核会向写进程发送 `SIGPIPE` 信号，默认导致写进程崩溃。
    *   如果写端全部关闭，读端读管道时，读完缓冲区剩余数据后，`read` 会返回 0（表示 EOF，文件结束）。

#### 2. 有名管道
*   **特点**：
    *   在文件系统中存在一个实体文件（类型为 `p`），但文件中不存数据，数据依然在内核缓冲区。
    *   允许**无亲缘关系**的进程相互通信。
*   **API**：
    *   创建：`int mkfifo(const char *pathname, mode_t mode);`
    *   操作：使用标准的 `open`, `read`, `write`, `close`。
*   **行为特性**：
    *   默认情况下，`open` FIFO 是**阻塞**的。如果以只读打开，会阻塞直到有进程以写方式打开它；反之亦然。

---

### 三、 信号

信号是一种**软件中断**，提供了一种处理异步事件的机制。它是在软件层次上对硬件中断机制的模拟。

#### 1. 信号的特点
*   **异步性**：进程无法预知信号到达的确切时间，只能在信号到达时被内核打断去处理。
*   **内核代发**：信号通常由内核代替进程发送，可以是硬件异常触发（如除零 `SIGFPE`），可以是键盘输入（`Ctrl+C` 触发 `SIGINT`），也可以是其他进程通过命令/函数发送。

#### 2. 信号相关函数（紧扣大纲）

**A. `kill` 函数：向指定进程发送信号**
```c
#include <sys/types.h>
#include <signal.h>
int kill(pid_t pid, int sig);
```
*   `pid > 0`：发送给指定 PID 的进程。
*   `pid == 0`：发送给与发送进程同进程组的所有进程。
*   `pid == -1`：发送给发送进程有权限发送的所有进程（广播）。
*   `sig == 0`：常用于**检测进程是否存在**。不实际发送信号，但会做权限检查，若目标进程不存在，返回 -1 且 `errno == ESRCH`。

**B. `raise` 函数：向自己发送信号**
```c
#include <signal.h>
int raise(int sig);
```
等价于 `kill(getpid(), sig)`。

**C. `alarm` 函数：定时发送信号**
```c
#include <unistd.h>
unsigned int alarm(unsigned int seconds);
```
*   在 `seconds` 秒后，内核会向当前进程发送 `SIGALRM` 信号。
*   默认动作是终止进程。如果之前设置过闹钟，会返回剩余秒数并替换之前的设定；`alarm(0)` 表示取消之前设定的闹钟。

**D. 信号处理函数注册（补充）**
```c
#include <signal.h>
// 古老且不安全的注册方式，不推荐
void (*signal(int signum, void (*handler)(int)))(int);

// 现代推荐的注册方式
int sigaction(int signum, const struct sigaction *act, struct sigaction *oldact);
```
*   `sigaction` 更健壮，支持在处理信号期间屏蔽其他信号，且能获取更多上下文信息。

#### 3. 信号处理的经典示例
```c
#include <stdio.h>
#include <signal.h>
#include <unistd.h>

void handle_sigalrm(int sig) {
    printf("收到了定时器信号 SIGALRM!\n");
}

int main() {
    // 注册信号处理函数
    struct sigaction act;
    act.sa_handler = handle_sigalrm;
    sigemptyset(&act.sa_mask);
    act.sa_flags = 0;
    sigaction(SIGALRM, &act, NULL);

    printf("设置 2 秒后的闹钟...\n");
    alarm(2);

    // 等待信号到来
    pause(); // 将进程挂起，直到捕获到一个信号

    printf("进程退出。\n");
    return 0;
}
```

---

### 四、 补充与拓展（进阶必知）

#### 1. 不可重入函数与信号处理的安全问题
信号处理函数运行在异步上下文中。如果主进程正在执行 `malloc`（申请堆内存，持有全局锁），此时收到信号中断，进入信号处理函数，而在处理函数中又调用了 `printf`（内部也可能分配内存，再次获取同一把锁），就会导致**死锁**。
**规则**：信号处理函数中只能调用**异步信号安全函数**。绝对避免调用 `malloc`, `printf`, 标准库 I/O 函数。推荐使用 `write` 输出，或者只设置一个 `volatile sig_atomic_t` 类型的标志位，在主循环中检查该标志。

#### 2. 信号的“生命周期”与不可靠性
*   早期 Unix 信号是不可靠的，信号可能会丢失。现代 Linux 改进了这一点。
*   **不可阻塞信号**：`SIGKILL` (9) 和 `SIGSTOP` (19) 是无法被捕获、阻塞或忽略的。这是为了给系统管理员提供一个最终极的杀进程手段。

#### 3. System V IPC 三剑客
在管道和信号之后，Unix System V 引入了三种更高级的 IPC 机制，它们在现代服务器开发中依然广泛使用：
*   **消息队列**：克服了管道的无格式字节流问题，进程可以读取特定类型/优先级的消息。
*   **共享内存**：速度最快的 IPC。多个进程将同一块物理内存映射到自己的虚拟空间。但它**不提供同步机制**，必须配合信号量或互斥锁使用。
*   **信号量**：之前详细讲过，用于进程间同步资源配额。

#### 4. 现代 IPC 王者：Unix Domain Socket (UDS)
*   虽然管道简单，但半双工且无格式；System V IPC API 繁琐且不易跨平台。
*   **UDS**（本地套接字）提供了全双工、带边界的数据流通信。它在 API 上与网络 Socket 完全一致，但不经过网络协议栈，直接在内核缓冲区拷贝，效率极高。
*   **现代实战**：Docker daemon 与 Docker CLI 之间的通信，以及 Android 系统的底层 Binder/Socket 通信，大量采用了 UDS。在 Linux 本机进程通信中，UDS 是目前的绝对主流。