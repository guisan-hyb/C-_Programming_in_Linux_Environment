由于同一进程内的线程天然共享内存空间，线程间通信的本质往往不是“如何传递数据”，而是**“如何协调访问共享数据的顺序”，即同步与互斥**。

以下是详细的讲解与拓展：

---

### 一、 线程通讯与同步概念

*   **互斥**：当多个线程访问同一份共享资源时，保证在任意时刻只有一个线程在访问。典型场景：多个线程写同一个全局计数器。
*   **同步**：多个线程按照约定的先后次序执行。典型场景：线程 A 产生数据，线程 B 处理数据，B 必须等 A 产生完才能运行。
*   **死锁**：多个线程互相等待对方释放资源，导致所有线程都无法继续执行。

---

### 二、 互斥相关

互斥锁是保护共享资源最基础的工具。

#### 1. 互斥锁
*   **概念**：像一把门锁，谁先拿到钥匙谁进去，其他人必须在门外排队（阻塞睡眠）。
*   **POSIX API**：
    ```c
    pthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;
    pthread_mutex_lock(&mutex);   // 加锁（拿不到就阻塞）
    // 临界区：操作共享数据
    pthread_mutex_unlock(&mutex); // 解锁
    ```
*   **拓展：尝试加锁与超时加锁**
    *   `pthread_mutex_trylock`：非阻塞获取，拿不到立即返回 `EBUSY`。
    *   `pthread_mutex_timedlock`：限时获取，超时返回 `ETIMEDOUT`。

#### 2. 自旋锁
*   **概念**：与互斥锁不同，当拿不到锁时，线程**不会睡眠**，而是在一个死循环中不断检查锁是否被释放（“自旋”）。
*   **适用场景**：临界区极短（只有几条指令）。因为睡眠和唤醒需要陷入内核态，开销很大；如果临界区极短，自旋等待反而更快。
*   **致命缺点**：如果临界区很长，自旋会白白浪费大量 CPU 资源。在单核 CPU 上，如果持有自旋锁的线程被抢占，等待自旋锁的线程会永远死循环（因为没人在跑）。

#### 3. 读写锁
*   **概念**：区分读和写。允许多个线程同时读，但只允许一个线程写。写的时候不能读。
*   **状态**：读锁（共享锁）、写锁（排他锁）。
*   **适用场景**：读多写少的场景（如缓存系统）。
*   **痛点（写饥饿）**：如果有大量读线程不断获取读锁，写线程可能会一直拿不到写锁而被饿死。可以通过设置写优先策略缓解。

---

### 三、 条件变量

互斥锁只能解决互斥，但无法解决同步问题（比如 B 线程必须等 A 线程完成）。条件变量专门用于线程间的**同步**。

#### 1. 核心机制
条件变量允许线程在某个条件不满足时挂起睡眠，直到另一个线程改变了条件并唤醒它。**条件变量必须配合互斥锁使用。**

#### 2. POSIX API
```c
pthread_cond_t cond = PTHREAD_COND_INITIALIZER;
pthread_mutex_t mutex;

// 线程 B (等待方)
pthread_mutex_lock(&mutex);
while (条件不满足) {
    // 解锁 mutex，然后当前线程睡眠等待 cond
    // 被唤醒时，重新加锁 mutex，继续向下执行
    pthread_cond_wait(&cond, &mutex); 
}
// 条件满足，执行任务
pthread_mutex_unlock(&mutex);

// 线程 A (通知方)
pthread_mutex_lock(&mutex);
// 改变条件...
pthread_cond_signal(&cond); // 唤醒至少一个等待在 cond 上的线程
pthread_mutex_unlock(&mutex);
```

#### 3. 拓展：为什么 `pthread_cond_wait` 必须放在 `while` 循环里？（虚假唤醒）
*   **概念**：某些操作系统或多核环境下，线程可能在没有收到 `signal` 的情况下从 `wait` 中醒来，或者一个 `signal` 唤醒了多个线程（惊群效应）。
*   **原因**：这是 POSIX 标准允许的现象。为了程序的健壮性，醒来后必须**再次检查条件**，所以必须用 `while` 而不是 `if`。

---

### 四、 信号量

信号量由 Dijkstra 发明，功能非常强大，既能用于互斥，也能用于同步。

#### 1. 概念与 P/V 操作
*   信号量本质是一个非负整数计数器。
*   **P 操作 (Proberen, 测试 / Wait)**：将计数器减 1。如果减 1 后变为负数，当前线程阻塞睡眠。
*   **V 操作 (Verhogen, 增加 / Post)**：将计数器加 1。如果加 1 前是负数（说明有线程在睡眠），唤醒一个等待的线程。
*   **物理意义**：计数器代表**可用资源的数量**。

#### 2. POSIX 信号量分类
分为**无名信号量**（基于内存，用于相关线程间）和**有名信号量**（基于文件系统，用于无亲缘关系进程间）。

#### 3. 无名信号量 API
```c
#include <semaphore.h>
sem_t sem;

// 初始化：pshared=0 表示仅在线程间共享，value 为初始资源数
sem_init(&sem, 0, 1); 

// P 操作
sem_wait(&sem); 

// 临界区

// V 操作
sem_post(&sem); 

// 销毁
sem_destroy(&sem);
```

#### 4. 信号量的两种用法
*   **用作互斥**：初始值为 `1`。P 操作加锁，V 操作解锁。相当于互斥锁。
*   **用作同步**：初始值为 `0`。
    *   线程 B 先执行 `sem_wait(&sem)`，因为值为 0，B 睡眠。
    *   线程 A 执行完前置任务后，执行 `sem_post(&sem)`，值变为 1，唤醒 B。
    *   完美实现了“A 先执行，B 后执行”的同步逻辑。

---

### 五、 信号量操作进阶与练习

#### 1. 同步练习：经典的生产者-消费者模型
利用信号量可以非常优雅地实现带有容量限制的生产者-消费者模型：

```c
sem_t sem_empty; // 空位数，初始值为 N (容器容量)
sem_t sem_full;  // 已生产数，初始值为 0
pthread_mutex_t mutex; // 保护容器的互斥锁

// 生产者
void producer() {
    sem_wait(&sem_empty);   // P(空位)，空位 -1。没空位就阻塞。
    pthread_mutex_lock(&mutex);
    // 放入数据
    pthread_mutex_unlock(&mutex);
    sem_post(&sem_full);   // V(产品)，产品 +1。唤醒消费者。
}

// 消费者
void consumer() {
    sem_wait(&sem_full);    // P(产品)，产品 -1。没产品就阻塞。
    pthread_mutex_lock(&mutex);
    // 取出数据
    pthread_mutex_unlock(&mutex);
    sem_post(&sem_empty);   // V(空位)，空位 +1。唤醒生产者。
}
```
*   **拓展**：如果用条件变量实现，逻辑会复杂得多（需要手动判断容器是否为空/满，且容易产生死锁）。信号量通过两个计数器天然解决了资源统计问题。

#### 2. 拓展：信号量与互斥锁的本质区别
虽然初始值为 1 的信号量可以当互斥锁用，但两者在概念上有严格区别：
*   **所有权概念**：互斥锁谁加锁，就必须谁解锁。而信号量没有所有权概念，线程 A 可以 `sem_wait`，线程 B 可以 `sem_post`。
*   **状态判定**：互斥锁永远处于锁定或解锁状态。而信号量可以记录多个状态（计数值）。

---

### 六、 有名信号量（课外拓展与进程间通信）

由于图片大纲中特别提到了“有名信号量”，这里做必要补充。

*   **产生背景**：无名信号量存在于内存中，只能用于同一进程的线程间，或者调用了 `fork` 的父子进程间（子进程继承父进程的无名信号量）。
*   **有名信号量**：通过名字（类似文件路径名 `/mysem`）在内核中创建，任何有权限的进程都能获取它。
*   **API**：
    ```c
    sem_t *sem = sem_open("/mysem", O_CREAT, 0644, 1);
    sem_wait(sem);
    sem_post(sem);
    sem_close(sem);
    sem_unlink("/mysem");
    ```
*   **应用场景**：多进程环境下的同步。例如 Nginx 的多进程 Worker 模型在启动时，就可以用有名信号量来协调各个 Worker 进程的启动顺序。

---

### 七、 总结与实战避坑

1.  **加锁粒度**：锁的粒度越细，并发度越高，但管理复杂度也越大。通常在保证正确性的前提下，尽量缩小临界区。
2.  **死锁的四大必要条件**：互斥、请求与保持、不剥夺、循环等待。破坏其中之一即可避免死锁。最实用的做法是**固定加锁顺序**（如按内存地址从小到大加锁）。
3.  **现代 C++ 视角**：
    *   现代 C++ 极力推荐使用 RAII 机制管理锁，如 `std::lock_guard`、`std::unique_lock`，避免忘记解锁导致死锁。
    *   C++20 引入了 `std::counting_semaphore`，将信号量纳入了标准库。
    *   **终极方向是无锁编程**：基于 `std::atomic` 原子操作和 CAS（Compare-And-Swap）算法，实现无等待的数据结构，彻底摆脱锁带来的睡眠开销和死锁风险。