在 Linux C/C++ 中，**文件 I/O** 是指直接调用 Linux 操作系统内核提供的**系统调用**来进行的输入输出操作。它与前面讲的标准 I/O（C 库函数）不同，文件 I/O **没有用户态缓冲区**，它直接与内核的页缓存交互。

以下是对 Linux 文件 I/O 及其核心操作的详细总结，并包含底层的深度拓展。

---

### 一、 文件 I/O 的核心概念

1.  **文件描述符**：
    *   文件 I/O 的核心句柄。它是一个**非负整数**（通常从 3 开始，因为 0、1、2 默认分配给了 `stdin`、`stdout`、`stderr`）。
    (文件描述符从0开始分配，一次递增，且每个进程独立分配，比如进程1描述符从0开始分配，进程2描述符也是从0开始分配)
    
    *   本质上是内核中“进程打开文件表”的索引。
2.  **无用户态缓冲**：
    *   调用 `write` 或 `read` 时，数据直接在你的变量（数组）和**内核态缓冲区（页缓存）**之间拷贝。
3.  **万物皆文件**：
    *   在 Linux 中，普通文件、目录、设备（键盘、鼠标、硬盘）、套接字、管道，全部通过文件 I/O 以 `fd` 的形式进行操作。

---

### 二、 文件 I/O 的核心操作 (Open, Read, Write, Close)

#### 1. 打开文件：`open()`
```c
#include <fcntl.h>
#include <sys/stat.h>

int open(const char *pathname, int flags);
int open(const char *pathname, int flags, mode_t mode);
```
*   **flags (必选)**：访问模式。
    *   `O_RDONLY` (只读), `O_WRONLY` (只写), `O_RDWR` (读写)。
*   **flags (可选，按位或 `|`)**：
    *   `O_CREAT`：文件不存在则创建。**必须提供 `mode` 参数**。
    *   `O_EXCL`：常与 `O_CREAT` 配合，如果文件已存在则报错（用于确保互斥创建）。
    *   `O_TRUNC`：如果文件存在且是写权限，将文件长度截断为 0（清空）。
    *   `O_APPEND`：每次写入前，自动将文件偏移量移到文件末尾（原子操作）。
*   **mode**：创建新文件时的权限（如 `0644`）。受进程 `umask` 掩码影响，最终权限为 `mode & ~umask`。
*   **返回值**：成功返回文件描述符 `fd`；失败返回 `-1` 并设置 `errno`。

#### 2. 关闭文件：`close()`
```c
#include <unistd.h>
int close(int fd);
```
*   释放文件描述符，并释放内核中相关的数据结构。
*   **注意**：进程结束时，内核会自动关闭所有打开的文件。但**显式关闭是极好的编程习惯**，因为系统每个进程允许打开的 `fd` 数量是有限的（默认通常是 1024）。

#### 3. 读取数据：`read()`
```c
ssize_t read(int fd, void *buf, size_t count);
```
*   从 `fd` 读取最多 `count` 个字节到用户态数组 `buf` 中。
*   **返回值极其重要**：
    *   `> 0`：实际读取的字节数（可能小于 `count`，比如遇到文件尾或网络包较小）。
    *   `= 0`：表示到达文件末尾（EOF）。
    *   `-1`：出错（例如被信号打断 `EINTR`，应重试）。

#### 4. 写入数据：`write()`
```c
ssize_t write(int fd, const void *buf, size_t count);
```
*   将 `buf` 中的 `count` 个字节写入 `fd`。
*   **返回值**：
    *   `> 0`：实际写入的字节数（通常等于 `count`，但写入网络或管道时可能小于）。
    *   `-1`：出错（常见错误：磁盘满 `ENOSPC`）。

#### 5. 移动文件指针：`lseek()`
```c
off_t lseek(int fd, off_t offset, int whence);
```
*   `whence`：`SEEK_SET` (开头), `SEEK_CUR` (当前), `SEEK_END` (末尾)。
*   **应用场景**：
    *   随机访问文件数据。
    *   `lseek(fd, 0, SEEK_END)` 常用来获取文件大小。
    *   **创建空洞文件**：如果 `lseek` 越过文件末尾再写入，中间会形成“空洞”（不占物理磁盘空间，读出来是 `\0`），常用于数据库预分配空间。

---

### 三、 文件 I/O 进阶拓展（核心难点与高频面试点）

#### 拓展一：数据一致性与同步
前面提到，`write` 只是把数据写入了内核页缓存，如果突然断电，数据会丢失。必须使用同步机制：
1.  `fsync(int fd)`：**强制**将 `fd` 关联的脏页缓存及**元数据**（文件大小、修改时间等）真正刷入磁盘硬件。会阻塞等待磁盘完成。（数据库如 MySQL 极度依赖此函数保证数据不丢）。
2.  `fdatasync(int fd)`：只刷数据和文件大小，不刷修改时间等元数据。性能比 `fsync` 好。
3.  `sync()`：将全局所有脏页刷盘（系统关机前会调用）。
4.  **标志位 `O_SYNC`**：在 `open` 时加上，每次 `write` 都会同步等待数据落盘，极其影响性能。

#### 拓展二：直接 I/O (Direct I/O)
*   **标志位 `O_DIRECT`**：绕过内核页缓存，数据直接在用户态内存和磁盘之间传输。
*   **严苛的限制**：要求用户态缓冲区地址、读写大小、文件偏移量**必须是磁盘块大小（通常是 512 字节或 4KB）的整数倍**，否则报错 `EINVAL`。
*   **应用场景**：数据库引擎（如 Oracle、InnoDB）。它们自己实现了一套极为高效的缓存算法，不需要操作系统“多此一举”再缓存一层。

#### 拓展三：内存映射 I/O (`mmap`)
这是 Linux 上极其强大的 I/O 方式。
```c
void *mmap(void *addr, size_t length, int prot, int flags, int fd, off_t offset);
```
*   **原理**：将磁盘文件的一段，直接映射到进程的虚拟内存空间。**没有数据拷贝过程**！
*   **操作**：映射后，不需要调用 `read/write`，直接像操作普通数组/指针一样操作内存（如 `ptr[0] = 'A'`），内核会负责自动将修改同步到文件。
*   **优点**：
    1.  零拷贝：省去了内核空间到用户空间的数据拷贝。
    2.  共享内存：父子进程或无关进程可以通过 `mmap` 同一个文件，实现极高效率的进程间通信 (IPC)。
*   **缺点**：映射大文件时，遇到页错误会产生中断；处理异常（如文件被截断）会产生 `SIGBUS` 信号导致进程崩溃。

#### 拓展四：文件描述符的复制 (`dup` / `dup2` / `fcntl`)
*   `dup2(int oldfd, int newfd)`：将 `oldfd` 复制到 `newfd`。
*   **核心用途：重定向**。
    *   Shell 中的 `> log.txt` 是怎么实现的？就是先 `close(1)`（标准输出），然后 `dup2(fd, 1)`，这样程序的 `printf` 就会写到文件里。
    *   实现守护进程时，重定向 `stdin/stdout/stderr` 到 `/dev/null`。

#### 拓展五：原子操作
文件 I/O 提供了一些必须的原子操作，解决并发竞争问题：
1.  **`O_APPEND` 的原子性**：如果没有这个标志，多个进程同时写同一个文件，需要先 `lseek` 到末尾再 `write`，这两步之间可能被打断，导致数据覆盖。加上 `O_APPEND`，内核保证每次 `write` 时自动移动指针和写入数据是一步完成的，绝对安全。
2.  **`O_CREAT | O_EXCL`**：保证创建文件的原子性。如果两个进程同时创建同一个文件，只有一个会成功，另一个会报错，可用于实现简单的文件锁。
3.  **`pread` / `pwrite`**：原子化的“定位并读写”。多线程环境下共享同一个 `fd` 时，普通 `lseek` + `read` 会导致文件指针混乱，必须用 `pread`。

---

### 四、 总结：标准 I/O (`FILE*`) vs 文件 I/O (`fd`)

| 特性 | 标准 I/O (C库) | 文件 I/O (系统调用) |
| :--- | :--- | :--- |
| **句柄类型** | `FILE *` (流) | `int` (文件描述符) |
| **缓冲区** | 有用户态缓冲（全/行/无） | 无用户态缓冲（直达内核缓存） |
| **系统调用次数** | 少（被库函数封装合并） | 多（每次调用都进内核） |
| **跨平台性** | 极好（C标准，Windows/Linux通用）| 差（POSIX标准，仅类Unix系统） |
| **功能丰富度** | 较少（仅基础读写、格式化）| 极多（内存映射、直接IO、锁、设备控制） |
| **适用场景** | 文本处理、小文件读写、格式化输入输出 | 网络编程、设备驱动交互、数据库底层、高并发、大文件 |

**最佳实践建议：**
1.  日常写 C/C++ 处理文本文件，**优先用标准 I/O**，方便且不易出错。
2.  写网络程序（Socket）、操作硬件设备、或者需要极致性能（如自己写数据库）时，**必须用文件 I/O (`fd`)**。
3.  C++ 程序中，通常将底层的 `fd` 封装在 RAII 类中，利用析构函数自动调用 `close()`，防止文件描述符泄漏。

